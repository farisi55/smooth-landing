# Cloudflare Pages configuration for the static Next.js export.
# Use `wrangler pages deploy`, not `wrangler deploy`, for this project.
name = "smooth-ai"
pages_build_output_dir = "./out"
compatibility_date = "2026-06-22"
